/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['i.ibb.co', 'static.vecteezy.com', 'media.istockphoto.com', 't3.ftcdn.net', 'promomedia.agency'],
  },
};

export default nextConfig;
